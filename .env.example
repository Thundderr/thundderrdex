# Supabase project credentials (Settings -> API in the Supabase dashboard).
# Copy this file to .env.local and fill in the values. Both must be set for
# the login/cloud-sync features; the app runs local-only without them.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
